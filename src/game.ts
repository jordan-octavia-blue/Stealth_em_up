/**
 * Game entry point (`game.html`).
 *
 * Everything still lives behind the legacy bridge for now; importing it boots the game
 * exactly the way the old script-tag list did. Phase 2 onwards replaces lines in
 * `legacy-bridge.ts` with real modules imported from here.
 */
import './legacy-bridge';
